# 阶段 3 云端单图探针——任务交接

> 交接时间：2026-08-05。交接人：上一对话。接收人：下一对话。
>
> 目的：阶段 3（`计划_BioSR-MT全目录严格生产.md`）的云端单图探针尚未执行；本文件让下个对话无缝接手，第一件事是连接云端并确认工作目录。

## 1. 任务一句话

在云端用**新版本 napari-fluoresfm（`2fded7c` v0.3.4）** 以历史 GUI 参数推理 `WF_noise_level_3/41.tif`，与 bundled `_fluoresfm` 参考严格比对（shape/dtype/数组/SHA-256）；不严格时再用老版本 `d623403` 对照。推理只在云端跑，本机不跑。

## 2. 已确认决策（用户拍板）

- 2 个配置：**s2 = 新版本 `2fded7c`（首选）**；**s1 = 老版本 `d623403`（对照）**。不要 s3。
- 参数统一为历史 GUI 参数：`sf_lr=1`、`batch_size=4`、`patch_size=256`、`compile=true`（s1 的老版本源码写死 compile，忽略该参数）。
- 云端只访问个人 `lc` 目录；本地只动 `D:\Documents\AI\CV\FluoResFM`。不装本机环境。
- 不预压缩：用 rsync `-z` 传输层压缩，远端落盘保持原字节。
- 批量生产须按 mtime 分组（见 §4），不能单一版本硬套 90 张。

## 3. 下一步（按顺序执行，别停）

1. 读 `C:\Users\Angela\.ssh\config`（用户已明确授权此文件）→ 拿到云端主机别名与账户。
2. `ssh <别名>` 检查云端：`whoami; pwd; ls ~; nvidia-smi; df -h`；找 `FluoResFM`/`lc` 目录。
3. **确认工作目录**（预期 `~/lc/FluoResFM`）；核对云端是否已有 `example/` 数据与 checkpoint（有则跳过 3.4 GB 重传）。
4. 本地同步（先 dry-run）：
   ```bash
   python scripts/sync_probe_to_server.py --host <别名> --user <账户> --include-probe-data --dry-run
   python scripts/sync_probe_to_server.py --host <别名> --user <账户> --include-probe-data
   ```
5. 云端先 `--dry-run` 复核，再正式跑首选 s2：
   ```bash
   cd <工作目录>
   python scripts/probe_biosr_mt_fluoresfm_server.py --config configs/biosr_mt_fluoresfm_server_probe_20260805_s2.json
   ```
6. 读 `comparison.csv` / `manifest.json`：全部 `strict` → 进入批量生产规划；否则 checkout `d623403` 跑 s1 对照。
7. 两组都不严格 → 如实报告“无法在现有证据下严格生产”，不得选误差最小的候选。

## 4. 已核实事实（有证据）

| 项 | 值 |
| --- | --- |
| FluoResFM gitlink 固定子模块 | `2fded7c31be8c52476de89934ced9093ebe2c307`（v0.3.4），工作树干净 |
| 老版本 `d623403` | 2025-07-26 11:53 "add function"，比 `2fded7c` 老 38 个提交，在 main 上 |
| 新版本 `2fded7c` | 2026-01-02 "add classifier"，napari-fluoresfm 最后一次提交 |
| `predict.py` 差异 | `d623403` 写死 `complie_model=True`、先载权重再 compile、无 matmul precision 设置；`2fded7c` 接受 `compile` 参数、先 compile 再载权重、设 `float32_matmul_precision("high")` 与 `cudnn.benchmark=True` |
| `_fluoresfm` mtime | level_1=07-26 11:35；level_3=07-28 16:11；level_4=07-28 23:51；level_5=07-29 00:10；level_2/7=08-09 —— 至少 4 个生成时段 |
| 输入 41.tif SHA-256 | `8c437b43b68a3281d060adc821b57566ddbaedf3f12d48fc0b67b2e4120dc202`（float32，存储形状 `[1,502,502]`） |
| 参考 41.tif SHA-256 | `db01b963bd3d4de55bf43562cb79677ebcb555b283978ba11e97bc360c3f0f4b` |
| 体积 | 小文件集 35.7 KB；子模块 72 文件 3.6 MB；`example/data/BioSR_MT` 83,591 文件 1,573.5 MB；`example/checkpoints` 12 文件 3,355.5 MB（`epoch_0_iter_700000.pt` 2,608 MB、`open_clip_pytorch_model.bin` 747.4 MB，均 zip 未压缩存储） |
| 本机环境 | RTX 3060 Laptop（驱动 610.62），Python 3.12.13（conda `napari-fluoresfm`），torch 2.6.0+cu124，triton-windows 3.7.1.post27，open_clip_torch 3.3.0，tifffile 2026.7.14；用户确认本机不跑推理 |
| 传输工具 | WSL rsync 3.4.1 可用；原生 rsync 缺失；OpenSSH ssh/scp 可用 |

## 5. 已交付文件（本会话产出，未提交 git）

| 文件 | 说明 | 状态 |
| --- | --- | --- |
| `scripts/probe_biosr_mt_fluoresfm_server.py` | 服务器单图探针（schema v1）；退出码 0=strict/1=数值等价/2=失败/3=运行错误；`--dry-run` 不加载 torch；拒绝覆盖；失败落盘 `failure.json` | 已校验（py_compile + dry-run） |
| `configs/biosr_mt_fluoresfm_server_probe_20260805_s1.json` | 老版本 `d623403` 对照 | 已校验（policy + dry-run） |
| `configs/biosr_mt_fluoresfm_server_probe_20260805_s2.json` | 新版本 `2fded7c` 首选 | 已校验（policy + dry-run，本地版本匹配） |
| `docs/plans/清单_阶段3_服务器单图探针.md` | 9 节执行清单，含云端传输、环境冻结、判读/停止条件 | 已更新 |
| `scripts/sync_probe_to_server.py` | 一键同步：优先 WSL rsync、回退 scp；三档（默认 / `--include-probe-data` / `--include-data`） | 已校验（dry-run 输出命令正确） |
| `scripts/README.md` | 新增两个脚本条目 | 已更新 |

基线参考（勿覆盖）：`data/derived/biosr-mt-example-verified-assets/20260805_r3/baseline/reference_inventory.csv`。

## 6. 边界与纪律

- 本地只读写 `D:\Documents\AI\CV\FluoResFM`；云端只读写个人 `lc` 目录；禁止共享/他人目录、禁止 sudo。
- 不覆盖 `example/`、checkpoint、既有审计证据；探针与同步脚本均只写新目录/只推送。
- 云端依赖装在个人隔离环境（或经管理员批准）；不在服务器装/升级共享依赖。
- 每次运行记录：输入 SHA-256、脚本/配置版本、Git commit、Python/Torch/CUDA/驱动、GPU、种子、命令、stdout/stderr、逐文件比较。
- 不 `git commit`（除非用户明确要求）。

## 7. 给下个对话的行为要求（用户在做“对话缺陷 vs 模型缺陷”测试）

- 开场白之后必须**同一回合内**紧跟工具调用；绝不以“我马上要…”作为回合结尾。
- 收到“继续/可以”等放行指令立即执行，不复述计划、不再确认。
- 工具失败当场修复并重跑，至少重试一次后才汇报。
- 回合只在“任务完成”或“必须用户提供信息”时结束；结尾给结果，不给计划。

## 8. 未决问题（需在云端确认）

- 云端主机别名/账户（读 `C:\Users\Angela\.ssh\config`）。
- 云端是否已有 `example/` 数据与 checkpoint（避免重复传输）。
- 云端 Python/Torch/Triton 环境是否齐备、`torch.compile` 是否可用。
- 最终工作目录路径（预期 `~/lc/FluoResFM`，以实际为准）。
