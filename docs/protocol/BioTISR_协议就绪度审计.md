# BioTISR 外部 SR 协议就绪度审计

> 状态：已完成；证据等级：协议核验（不构成模型实验）；日期：2026-07-28。

## 范围与方法

- 元数据：上游 `repos/fluoresfm/dataset_test-v2.xlsx`，SHA-256 为 `2D3571468CF8869EF539A49CE979056712FE9539FB59601BA4810D8547851CAA`。
- 检查对象：工作簿中 12 个规范外部 BioTISR 超分辨率条目（CCP、F-actin、lysosome、MT，各 3 个 level）。
- 数据根：本地现有 `data/raw/BioTISR/`。审计脚本将工作簿中的作者机器绝对路径重映射到该根目录，并逐项检查索引、输入图、参考图及索引内逐文件配对。
- 工件：`experiments/biotisr_protocol_audit/20260728_raw_root_r1/readiness.json`（Git 忽略）。未修改工作簿、原始数据或预测。

## 结果

| 项目 | 结果 |
|---|---:|
| 规范 SR 条目数 | 12 |
| 索引存在 | 0/12 |
| 输入目录存在 | 0/12 |
| 参考目录存在 | 0/12 |
| 逐文件配对就绪 | 0/12 |

本地仅发现 RawSIM BioTISR 原始包，未发现工作簿预期的 `transformed/` 输入/参考图目录树。因此不能将现有 RawSIM 文件直接当作工作簿定义的外部 SR 测试输入，也不能运行 P1/P0 级别的零样本评估。

## 可支持表述与边界

可支持的结论仅是：在当前工作区，12 个规范 BioTISR 外部 SR 条目均未达到逐文件可追溯的评估就绪状态。该结果不说明数据不可获取，也不说明 RawSIM 包无效；更可能的解释是作者使用的转换产物或处理流程尚未提供到本地。

下一步需要新增的外部材料是：作者已转换的测试集，或可审计的 RawSIM→WF/GT 转换流程及其参数、索引与逐文件映射。在此之前，停止将 BioTISR CCP 训练目录结果称为独立测试或近似论文复现。
