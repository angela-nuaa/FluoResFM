# Pre-trained model weights and downloaded assets (do not version large or
# redistribution-restricted artifacts; record their provenance in
# DATA_AND_MODELS.md instead).
pretrained_weights/
example/checkpoints/
example/data/
example/image retrival/database.npy
*.pth
*.ckpt
*.pt
*.bin

# 数据
data/

# 实验结果
experiments/
outputs/
results/
# Publish only compact, machine-readable result indexes. Raw result folders,
# TIFF files and per-file metrics remain ignored.
!results/
!results/README.md
!results/*.summary.json
runs/
wandb/

# Jupyter Notebook 缓存
notebooks/.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
*.egg
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
desktop.ini

# 日志
*.log

# 个人本地文档（不提交）
.local/

# example
example/
example.tar

# Local asset provenance may include private download locations or notes. Keep
# the tracked example manifest in assets/manifest.local.example.json instead.
assets/manifest.local.json

# Local cloud-operation helpers may contain machine-specific connection details.
# Keep credentials in a user-managed secret store, never in this repository.
scripts/_ssh_*.py
